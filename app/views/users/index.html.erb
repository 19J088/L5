<% if flash[:notice] %>
    <%= flash[:notice] %>
<% end %>

<% @user.each do |e| %>
    <p>
        <%=  e.uid %>
        <%= e.pass %>
        <%= link_to '削除', "/users/#{e.id}" , method: :delete %>
        </p>
    
<% end %>
<p><%= link_to 'ホームへ', '/' %></p>
